export enum Role {
  Member = 0,
  Checker = 1,
  Admin = 2
}

export enum Status {
  Waiting = 0,
  Accepted = 1
}

export enum StatusChallenge {
  InProgress = 0,
  Waiting = 1,
  Validated = 2,
  Rejected = 3
}
